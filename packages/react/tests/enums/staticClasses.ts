import { isAlignable, isJustifiable} from 'aspire-components-helpers'

export const justifiable = {
  method: isJustifiable,
  field: 'justify'
}

export const alignable = {
  method: isAlignable,
  field: 'align'
}
